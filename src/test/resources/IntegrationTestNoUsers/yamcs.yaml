instances:
 - IntegrationTestNoUsers
 #- IntegrationTest2

services:
  - class: org.yamcs.http.HttpServer
    args:
      port: 9191

incomingDir: /tmp/yamcs-incoming
dataDir: /tmp/yamcs-IntegrationTestNoUsers-data

# secretKey: testtest_nousers
